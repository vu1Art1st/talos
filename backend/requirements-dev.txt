-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
# aiosqlite（SQLite 异步驱动）已随 2026-09-21「统一 PostgreSQL 单栈」收口移除；
# 测试库自该日起为 PostgreSQL（asyncpg）。如需追溯旧实现，见 git 历史。

# 静态检查（2026-09-17 审计引入）：ruff = lint（未使用导入/长行等）；vulture = 死代码
ruff>=0.16
vulture>=2.16

# 测试流程改进（2026-09-21）：
# - xdist：每个 worker 是独立进程，配合 conftest「每进程独占 schema」可安全并行；
# - cov：覆盖率按「增量」解读（CI 只报告不设全量阈值，见 AGENTS.md 验收口径）。
pytest-xdist>=3.6
pytest-cov>=5.0
