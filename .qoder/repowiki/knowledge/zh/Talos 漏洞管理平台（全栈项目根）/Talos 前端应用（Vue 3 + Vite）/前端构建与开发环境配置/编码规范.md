- 所有开发相关命令统一通过 package.json 的 scripts 字段声明，不单独维护 Makefile 或 shell 脚本。
- 环境变量命名采用 VP_ 前缀（如 VP_FRONTEND_PORT、VP_BACKEND_PORT）用于覆盖构建与开发行为。
- Tailwind 样式通过 class 模式控制暗色主题，并在 theme.extend 中集中定义品牌色、圆角、阴影与字体族。
- 第三方样式库（Element Plus）通过禁用 Tailwind preflight 来避免样式冲突，保持 UI 一致性。