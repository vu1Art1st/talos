- 版本发布记录遵循 Keep a Changelog 格式，使用 `[Unreleased]` 段落暂存未发布变更，发布时移入对应语义化版本号段落并按 Added/Changed/Fixed/Removed/Security 分类组织。
- 截图文件采用两位数字序号前缀加功能描述的命名约定（如 `01-login.png`、`05-vuln-form.png`），并在 USER_GUIDE.md 中以相对路径 `images/文件名` 引用。
- 文档中使用反引号包裹的命令、环境变量名（如 `VP_SECRET_KEY`、`docker compose up -d`）和文件路径，保持与仓库实际结构一致。
- ROADMAP.md 每项规划统一包含「场景」「核心流程」「涉及模块」三段式结构，并按优先级从高到低排列。