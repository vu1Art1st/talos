所有脚本需在已配置好 `.env` 且运行 `docker compose` 的服务器仓库根目录下执行，例如 `bash scripts/backup.sh`、`bash scripts/restore.sh backups/<时间戳>`、`bash scripts/migrate.sh`、`bash scripts/upgrade.sh [--no-backup] [--no-pull]`。`restore.sh` 要求目标机器为全新空库（刚 `docker compose up` 过或未初始化）。