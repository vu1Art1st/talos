- 后端配置统一通过 VP_* 环境变量注入，docker-compose 中集中声明，本地开发通过脚本设置 VP_DISABLE_QUEUE=1 切换为 SQLite 免队列模式。
- 前后端通过 REST API 解耦，前端所有后端调用集中在 src/api 目录，使用统一的请求封装与错误处理。
- 项目根目录的 dev.* 脚本负责环境初始化（虚拟环境、node_modules）与服务启停，屏蔽各子模块差异。