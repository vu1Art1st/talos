- 应用初始化采用链式 `app.use()` 注册插件（Pinia、Router、ElementPlus），最后统一 `app.mount('#app')` 挂载。
- 全局样式集中在 `style.css`，并通过 Tailwind 的 `@tailwind base/components/utilities` 三段式引入，业务通用样式以 BEM 风格的类名（如 `.rich-content`、`.tiptap`）组织。
- Vue 组件使用 `<script setup lang="ts">` 语法糖，保持模板与逻辑在同一文件中声明。