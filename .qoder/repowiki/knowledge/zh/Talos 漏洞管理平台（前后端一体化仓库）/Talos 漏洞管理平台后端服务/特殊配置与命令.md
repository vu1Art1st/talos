运行：`uvicorn app.main:app --host 0.0.0.0 --port 8000`（Dockerfile 已内置）。后台任务单独启动：`arq app.workers.main.WorkerSettings`。数据库迁移：`alembic revision --autogenerate -m "描述"` 后 `alembic upgrade head`。测试：`pytest`（使用 `tests/test_vp.db` 作为测试数据库）。开发时可通过 `DISABLE_QUEUE=True` 禁用 Redis 队列，使后台任务在 API 进程内同步执行。